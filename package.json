{
  "name": "venia-agents",
  "private": true,
  "description": "VENIA OS — static app + Netlify functions. web-push powers the agent-finished notifications sent by netlify/functions/agent-background.js.",
  "dependencies": {
    "web-push": "^3.6.7"
  }
}
